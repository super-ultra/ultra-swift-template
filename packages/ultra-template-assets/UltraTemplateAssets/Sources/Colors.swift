import SwiftUI
import UltraTemplateCore

extension Color {
    public enum Assets {
        public static let background = Color(hexRGB: "#F5F5F5")
        public static let contentBackground = Color(hexRGB: "#FFFFFF").opacity(0.8)
        public static let majorTextColor = Color(hexRGB: "#111111")
    }
}
